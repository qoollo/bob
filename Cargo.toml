[workspace]
members = [
    "bob",
    "bob-apps",
    "bob-backend",
    "bob-common",
    "bob-access",
]


[profile.release-lto]
inherits = "release"
lto = true

[profile.integration-test]
inherits = "test"
opt-level = 1
debug = 0
debug-assertions = true
overflow-checks = true